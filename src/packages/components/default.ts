import type { LabelConfig, GroupConfig } from './types'

export const defaultHorizontalLabelConfig: LabelConfig = {
  width: '66px',
  align: 'right'
}

export const defaultVerticalLabelConfig: LabelConfig = {
  // width: '66px',
  align: 'left'
}

export const defaultGroupConfig: GroupConfig = {
  direction: 'horizontal'
}