/**
 * @description 最小值
 * @param {String} value
 * @param {String} minValue
 */ 
const minValue = (value: string, minValue: number): boolean => {
  return +value >= minValue
}

export default minValue